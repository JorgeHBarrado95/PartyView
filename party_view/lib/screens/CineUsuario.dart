import 'package:flutter/material.dart';

class CineUsuario extends StatelessWidget {
  const CineUsuario({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text("CINEEEE")));
  }
}
